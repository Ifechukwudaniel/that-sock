import React from "react";

const BackgroundImageComponent = ({ src, width, height }) => {
  return <img src={src} width={width} height={height} alt="socks" />;
};

export default BackgroundImageComponent;
